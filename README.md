# Terminal

Приложение **Terminal** позволяет отправлять, принимать и визуализировать данные, а также управлять командами через удобный интерфейс.

---

## 🚀 Возможности

- **Поддержка COM-портов** с различными протоколами:
  - `SimpleSerial`
  - `POESerial`
  - `POECanable`
  - `POECanableFD`
- **Отправка команд** в порт.
- **Сохранение и открытие файлов с командами** (в формате YAML).
- **Визуализация** полученных данных.
- **Гибкая настройка параметров** подключения (скорость, биты данных, стоп-биты и т.д.).
- **Отображение данных в формате графиков** (для протокола POESerial).

---

## 📦 Протоколы

Приложение поддерживает следующие протоколы:

### SimpleSerial

- **Формат**: `DATA + END_PACKAGE`
- **Пример**: `Restart\r\n`

### POESerial

- **Формат**: `[SOH] HEADER [US] ARGUMENT [STX] VALUE [ETX] CRC8 [EOT]`
- **Пример**: `[SOH] SET [US] RESTART [STX] {} [ETX] BA [EOT]`

### POECanable / POECanableFD

- **Формат**: `FRAME_TYPE + CAN_ID + DLC + DATA + \r`
- **Описание полей**:
  - **FRAME_TYPE**: `t`, `T`, `r`, `R`, `d`, `D`, `b`, `B`
    - `t` / `T`: CAN-фрейм данных (стандартный / расширенный)
    - `r` / `R`: CAN-фрейм удаленного запроса (стандартный / расширенный)
    - `d` / `D`: CAN-FD-фрейм данных с переменной скоростью (стандартный / расширенный)
    - `b` / `B`: CAN-FD-фрейм данных с фиксированной скоростью (стандартный / расширенный)
  - **CAN_ID**: идентификатор CAN (3–8 шестнадцатеричных символов)
  - **DLC**: длина данных (1 шестнадцатеричный символ)
  - **DATA**: данные (шестнадцатеричные байты, без пробелов)
  - **\r**: символ возврата каретки (CR)
- **Примеры**:
  - `t0401FFFF81122334455667788\r` — CAN-фрейм данных с ID `0x0401FFFF`, 8 байт данных.
  - `T123456784AABBCCDD\r` — расширенный CAN-фрейм данных с ID `0x12345678`, 4 байта данных.
  - `r0401FFFF\r` — CAN-фрейм удаленного запроса с ID `0x0401FFFF`, без данных.
  - `B12345678F00112233445566778899AABBCCDDEEFF\r` — CAN-FD-фрейм с ID `0x12345678`, 16 байт данных (CANFD_DLC_F).

---

## 🧰 Функции

- **Подключение к порту**: Выбор порта, настройка параметров и подключение.
- **Отправка команд**: Создание, сохранение и отправка команд в порт.
- **Управление командами**:
  - Создание новых команд.
  - Сохранение команд в YAML-файл.
  - Загрузка команд из файла.
- **Мониторинг данных**: Отображение полученных данных в реальном времени.
- **Графики**: Визуализация данных (для протокола POESerial).
- **Жёсткий перезапуск устройства** (через DTR/RTS).

---

## 📁 Файлы команд

Команды можно сохранять в YAML-файлы и загружать из них. Это позволяет:

- Быстро использовать часто используемые команды.
- Делиться командами между пользователями.
- Автоматизировать тестирование.

---

## 🛠️ Сборка и запуск

### Требования

- [Rust](https://www.rust-lang.org/)
- [Tauri CLI](https://tauri.app/v1/guides/getting-started/prerequisites)
- [Node.js](https://nodejs.org/en?spm=a2ty_o01.29997173.0.0.414d5171T6pvQe)
- [Svelte](https://svelte.dev/?spm=a2ty_o01.29997173.0.0.414d5171T6pvQe)

### Сборка

```bash
tauri build
```
